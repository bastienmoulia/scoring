import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bastienmoulia.scoring',
  appName: 'scoring',
  webDir: 'dist/scoring/browser'
};

export default config;
